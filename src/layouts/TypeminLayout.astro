---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const { title, description, ogImage } = Astro.props;
---

<BaseLayout title={title} description={description} ogImage={ogImage} brandClass="theme-typemin" navMode="main">
  <slot />
</BaseLayout>

<style is:global>
  .theme-typemin {
    --brand-accent: var(--cobalt-light);
  }
</style>
